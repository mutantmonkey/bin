#!/bin/sh
newenv=""

if [ -n "$GDK_SCALE" ]; then
    newenv+="GDK_SCALE=$GDK_SCALE\n"
fi

if [ -n "$GDK_DPI_SCALE" ]; then
    newenv+="GDK_DPI_SCALE=$GDK_DPI_SCALE\n"
fi

newenv+="export GDK_SCALE GDK_DPI_SCALE"
echo -e "$newenv" | ssh $1 sh -c 'cat > $HOME/.xprofile'
